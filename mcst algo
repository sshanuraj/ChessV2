root
expanded root

for num of iteration:
select highest --- >init all inf
random selection


isterminal()
backprop huge positive or negative reward

isleaf()
yes->rollout->backprop reward->update visits for each node
no->populatenode - > select max ucb - > rollout - >backprop - > update visits
